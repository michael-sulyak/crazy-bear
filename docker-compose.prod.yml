version: '3'

services:
  core:
    restart: always
    build:
      context: .
      dockerfile: ./Dockerfile
    env_file:
      - envs/prod.env
    privileged: true
    volumes:
      - .:/app
      - /dev:/dev
    ports:
      - "8443:8443"
    command: python3 __main__.py
